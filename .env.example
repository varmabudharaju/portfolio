# Copy this file to `.env` and fill in the values, then restart `npm run dev`.
# All three keys are PUBLIC (safe to expose in client-side code).

# --- Contact form (Web3Forms) ---
# Get a free access key at https://web3forms.com (verify sairam.vzf33@gmail.com).
VITE_WEB3FORMS_KEY=

# --- Arcade leaderboard (Supabase) ---
# From your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
